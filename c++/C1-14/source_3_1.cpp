//
// Created by puras on 2025/6/22.
//
#include<iostream>
using namespace std;

int main() {
    int num;
    cin >> num;
    int d, nn = 0;
    while(num != 0) {
        // 1、取输入数的个位数
        // 2、将输入数缩小10倍
        // 3、判断获取的个位数是不是0
        // 如果是0，跳过，什么也不做
        // 如果不是0
        // 新数放大10倍再加上获取的个位数
    }

    cout << nn;

    return 0;
}